-- API root set for the bulletproof-pcs package (the IPA polynomial commitment).
-- Consumed by the workspace dead-code pass (formal/scripts/deadcode.sh), which unions
-- every package's roots.txt. The axiom audit lives in scripts/check_axioms.lean.

-- Binding: exactly discrete-log-relation triviality among the generators, and the
-- opening is unique under binding.
Bulletproof.commitmentBinding_iff_no_relation
Bulletproof.ipaRelation_unique

-- The chunked openings — the PCS as kimchi deploys it (a single chunk is the
-- one-element instance). Single opening: an accepting run on the combined commitment
-- of a polynomial's chunks pins its evaluation, under binding and the Fiat-Shamir
-- rewinding hypothesis. Batched: acceptance at pairwise-distinct
-- (polyscale, evalscale) pairs binds every commitment family to one genuine wide
-- polynomial.
Bulletproof.chunked_ipa_soundness
Bulletproof.chunked_batch_soundness

-- The Fiat-Shamir junction: the executable wire verifier meets the soundness layer,
-- and the per-curve headline — a grid of Poseidon-accepted runs yields genuine openings
-- (composing the declared FS axioms, the flattening lemmas, and chunked_batch_soundness).
Bulletproof.verify_reflects
Bulletproof.ipaVesta_sound
Bulletproof.ipaPallas_sound

-- The declared trust surface.
Bulletproof.poseidon_fiat_shamir_vesta
Bulletproof.poseidon_fiat_shamir_pallas
