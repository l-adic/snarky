-- Library API root set for the kimchi package.
--
-- The curated top-level surface the library exists to provide. Lean has no export-list
-- mechanism (everything non-`private` is visible once imported), so this manifest — not the
-- type system — defines the public API. It is the root set for dead-code analysis:
--
--   a declaration in `Kimchi.*` is LIVE iff it is in the transitive constant-dependency
--   closure of these roots; anything compiled but outside that closure is dead.
--
-- Format: one fully-qualified declaration name per line; `--` lines and blanks are ignored.
--
-- Note: the `kimchi-demo` executable (Main.lean) is a SEPARATE build root, reaching the demo
-- defs `Kimchi.Gate.{egQ, egGood, egBad, satisfies}`. Those are demo-only and are not part
-- of this library API surface, so they are reachable through the executable rather than here.

-- Generic gate
Kimchi.Gate.Generic.sound
Kimchi.Gate.Generic.complete
Kimchi.Gate.satisfies_iff

-- Complete addition
Kimchi.Gate.AddComplete.complete
Kimchi.Gate.AddComplete.sound

-- Variable-base scalar multiplication (gate)
Kimchi.Gate.VarBaseMul.sound
Kimchi.Gate.VarBaseMul.complete

-- Variable-base scalar multiplication (circuit): the two deployed entry points
Kimchi.Gate.VarBaseMul.varBaseMul_scaleFast1
Kimchi.Gate.VarBaseMul.varBaseMul_scaleFast2

-- Endo-scalar decomposition (gate)
Kimchi.Gate.EndoScalar.sound
Kimchi.Gate.EndoScalar.complete

-- Endo-scalar decomposition (circuit): over a multi-row run of sequential gates, the effective
-- scalar a·λ + b (sound), the honest prover fills the run (complete), and the decomposition is
-- unique under the bit-size/field-size bound (so challenge ↦ a·λ + b is well defined)
Kimchi.Gate.EndoScalar.chain_toField
Kimchi.Gate.EndoScalar.chain_complete
Kimchi.Gate.EndoScalar.endoScalar_unique

-- GLV endomorphism scalar multiplication (gate)
Kimchi.Gate.EndoMul.sound
Kimchi.Gate.EndoMul.complete

-- GLV endomorphism scalar multiplication (circuit): the generic capstone over `Holds` — a run of
-- valid EndoMul rows + base + threading + initial conditions computes [s]·T, with intermediate
-- nonsingularity derived (no per-row bundle). Ties EndoScalar ↔ EndoMul ↔ [s]·T.
Kimchi.Gate.EndoMul.endoMul
-- GLV non-degeneracy at the Pasta curves: a bounded two-base accumulator avoids ±T/±φT (the
-- hxne core, via the GLV short-basis bound)
Kimchi.Gate.EndoMul.pallas_combo_off_targets
Kimchi.Gate.EndoMul.vesta_combo_off_targets
-- EndoMul at the real Pasta curves: the deployed entry points. The prover supplies only the gate
-- constraint Holds per row + base + threading + initial conditions; the eigenvalue is discharged
-- via the CM axiom and the per-row hxne is *derived* from the GLV bound (not assumed)
Kimchi.Gate.EndoMul.pallas_endoMul
Kimchi.Gate.EndoMul.vesta_endoMul

-- Kimchi quotient/vanishing argument (implementation checkpoint). Over the size-n evaluation
-- domain, a constraint polynomial is divisible by Z_H = X^n - 1 iff it vanishes on every row
-- (`zH_dvd_iff`); instantiated at the double generic gate: both constraint polynomials divisible
-- iff the gate's two constraints hold at every row of the table (`genericRows_iff_dvd`).
Kimchi.Quotient.zH_dvd_iff
Kimchi.Quotient.genericRows_iff_dvd

-- Kimchi quotient argument (soundness engine + gate lifts). Counting Schwartz–Zippel soundness:
-- one aggregation challenge outside `badAlphas` separates the aggregate into per-constraint
-- divisibility (`dvd_separation`), and one evaluation point outside `badZetas` pins each quotient
-- identity. The generic lift engine (`rows_iff_dvd_of`,
-- selector-gated `rowsSel_iff_dvd`) turns any eval bridge into a divisibility-iff-rows theorem;
-- instantiated per gate via cell maps and `constraints_map` naturality (`<G>.rows_iff_dvd`).
-- Headline per gate: for every selector-active row the gate's Holds follows
-- (`<G>.soundness`); the EC meaning is obtained by citing Gate.<G>.sound where consumed.
Kimchi.Quotient.dvd_separation
Kimchi.Quotient.rows_iff_dvd_of
Kimchi.Quotient.rowsSel_iff_dvd
Kimchi.Quotient.AddComplete.rows_iff_dvd
Kimchi.Quotient.VarBaseMul.rows_iff_dvd
Kimchi.Quotient.EndoMul.rows_iff_dvd
Kimchi.Quotient.AddComplete.soundness
Kimchi.Quotient.VarBaseMul.soundness
Kimchi.Quotient.EndoMul.soundness

-- Argument: the single primitive every gate's quotient lift instantiates (kimchi argument.rs:
-- an F-algebra-natural constraint family over the ArgumentEnv cell environment);
-- rows/selector-gated/soundness stated once over it.
Kimchi.Quotient.Argument.rows_iff_dvd
Kimchi.Quotient.Argument.soundness

-- EndoScalar quotient lift (the gate's constraints read over any commutative F-algebra —
-- the cPoly/dPoly coefficients enter through algebraMap F R).
Kimchi.Quotient.EndoScalar.rows_iff_dvd
Kimchi.Quotient.EndoScalar.soundness

-- Poseidon gate (sound = a satisfying row computes the 5-round permutation) and its quotient
-- lift, completing the six-gate unification over the Argument primitive.
Kimchi.Gate.Poseidon.sound
Kimchi.Gate.Poseidon.complete
Kimchi.Quotient.Poseidon.rows_iff_dvd
Kimchi.Quotient.Poseidon.soundness

-- Grand-product core (permutation argument, milestone 1): equal products of linear pair
-- factors force equal pair multisets (UFD over F[β]); two-variable grid pinning; the
-- composed headline turning field-level product checks at enough (β,γ) into multiset equality.
-- Permutation milestone 2: the accumulator recurrence with both boundary pins telescopes
-- into the grand-product equality the multiset core consumes.
Kimchi.Quotient.prod_eq_of_accumulator

-- Permutation milestone 3: the zkpm-gated aggregation and Lagrange-gated boundary pins,
-- under the derandomized quotient hypotheses, telescope into the grand-product equality
-- over the unmasked region.
Kimchi.Quotient.Permutation.soundness

-- Permutation milestone 4: copy soundness — grand-product equality at one challenge pair
-- (β, γ) outside the bad set forces the witness values to be invariant under the wiring (the
-- abstract core, and the kimchi headline composing milestones 1-3).
Kimchi.Quotient.copy_soundness
Kimchi.Quotient.Permutation.copy_soundness

-- Permutation milestone 5: the index instantiation — coset shifts give injective cell
-- addressing, region-preserving wirings restrict, interpolated sigma columns carry the
-- wiring semantics; copy soundness from the index data alone.
Kimchi.Quotient.Permutation.copy_soundness_wired
Kimchi.Quotient.multiset_eq_of_pairFactor_prod_eq
Kimchi.Quotient.multiset_eq_of_prod_eval


-- Kimchi verifier reflection (trust-free): the deployed `kimchiVerify` code path read into a
-- well-formed `ReflectedRun`, and the barycentric public-evaluation identity the verifier
-- computes shown equal to the interpolant form. Foundations for the executable soundness capstone.
Kimchi.Verifier.kimchiVerify_reflects
Kimchi.Verifier.barycentricPubEval_eq

-- Phase A3: quotient soundness at the index (per-family bridges consuming Index data).
Kimchi.Index.addComplete_soundness
Kimchi.Index.varBaseMul_soundness
Kimchi.Index.endoMul_soundness
Kimchi.Index.endoScalar_soundness
Kimchi.Index.poseidon_soundness
Kimchi.Index.generic_soundness
Kimchi.Index.copy_soundness

-- The arithmetization interface those bridges compose into: satisfiability of a row or of
-- the whole table against divisibility of the aggregate family, in both directions, plus
-- the unmasked-cell congruence. These are gated by `scripts/check_axioms.lean` and so are
-- part of the declared surface; listing them here keeps the two manifests in agreement.
Kimchi.Index.satisfies_iff_fullFamily_dvd
Kimchi.Index.satisfies_of_fullFamily_dvd
Kimchi.Index.fullFamily_dvd_of_satisfies
Kimchi.Index.gateMember_dvd_of_rowSatisfies
Kimchi.Index.rowSatisfies_of_evalCheck
Kimchi.Index.satisfies_of_evalCheck
Kimchi.Index.exists_quotient_of_satisfies
Kimchi.Index.satisfies_congr_unmasked

-- The capstone: executable-verifier acceptance ⟹ the index is satisfiable, at every challenge
-- tuple outside the bounded (counting Schwartz–Zippel) bad sets. Combines the Fiat-Shamir
-- junction, the counting quotient soundness, and the IPA special-soundness extraction.
-- `kimchiBundle_sound` is the idealized composition: the transcript bundle is the
-- special-soundness HYPOTHESIS (the tree-of-accepting-transcripts idiom), consumed by one
-- application of the core; the concrete Fiat-Shamir capstone discharges it.
-- `kimchiProof_sound_of_openings` is the openings-interface seam (forking b1): the shared
-- junction of the extraction models — the standard-model path (density → rectangle →
-- batch_soundnessA) and the algebraic-prover path (representations carried with messages) both
-- discharge it. Reference side = pure commitment-knowledge; consumer side = per-row openings.
Kimchi.Protocol.kimchiProof_sound_of_openings

-- The standard-model run-level capstones (grid path): a real accepted run's own sponge
-- challenges over the run's own commitments — unless the run's (β, γ, α, ζ) land in the four
-- bounded bad sets, the index is satisfiable. The special-soundness grid (accepting deployed
-- Ipa.verify batch openings at injectively-varied ξ/r) is a HYPOTHESIS; each node's transcript
-- tree is derived from poseidon_fiat_shamir_{vesta,pallas}. The quotient residue (t, hdeg, heq)
-- is the one explicit hypothesis beyond the grids. The intermediate capstones these compose
-- (kimchiProof_sound → kimchiBundle_sound → kimchi{Vesta,Pallas}_sound) are live via
-- reachability, no longer listed.
Kimchi.Verifier.kimchiVesta_run_sound
Kimchi.Verifier.kimchiPallas_run_sound

-- The algebraic-group-model reading: an algebraic prover supplies SRS-basis representations of
-- its committed rows, so a SINGLE accepted opening suffices (no grid, no density) — the eval
-- pins follow off a small discrepancy (ξ, r) bad set (a bivariate Schwartz–Zippel event, ≤ 84
-- and ≤ 1). Consumes the openings seam. NOTE these roots keep the FiatShamirTreeB tree as a
-- caller-supplied hypothesis (not derived from an Ipa.verify acceptance), so their axiom set is
-- the density capstones' MINUS poseidon_fiat_shamir; the ft/quotient identity is kept as the
-- residue hypothesis (dissolved by the `_ft` roots below).
Kimchi.Verifier.kimchiProof_sound_algebraic

-- The algebraic quotient: the ft/quotient residue DISSOLVED. The algebraic prover additionally
-- supplies the 7 `tComm`-chunk representations, so the quotient `t` — now the genuine
-- degree-`< 7n` assembly `ftChunkAssembly` of the committed chunks — and the Maller/ft identity
-- are DERIVED from a checked ft opening via `ft_identity_of_chunks`; the residue hypothesis is
-- gone from the statement. Axiom set unchanged from the algebraic roots above (the ft opening,
-- FiatShamirTreeB, and DL-binding remain hypotheses; still MINUS poseidon_fiat_shamir).
Kimchi.Verifier.kimchiProof_sound_algebraic_ft

-- The FS-reflection ft opening (PART 1 of the deployed-run discharge): the ft commitment is slot 1
-- of the run's own accepted 45-row batch, so its opening at ζ to `runFtEval0` is DERIVED from a
-- genuine `verify = true` — `ipa_soundnessA` + the general-arity `eval_pins_of_opening` at that
-- slot. `ft_opening_of_reflected` is axiom-free (the transcript tree is a hypothesis); the curve
-- wrappers discharge it via the new `kimchi_fiat_shamir_{vesta,pallas}` axiom (warm reflected run)
-- + `kimchiVerify_reflects`, so their closure is base + that axiom + CompElliptic (no
-- poseidon FS, no eigen). Deferred follow-on: reconcile the 45-row batch with `batchC` to feed
-- `kimchiProof_sound_algebraic_ft` and reach a full run-level residue-free `Satisfies` root.
Kimchi.Verifier.ft_opening_of_reflected
Kimchi.Verifier.ft_opening_of_reflected_vesta
Kimchi.Verifier.ft_opening_of_reflected_pallas

-- THE TERMINAL THEOREM — AGM soundness of the deployed kimchi verifier. From a genuine
-- `verify = true`, for an algebraic prover (SRS-basis representations of the run's 45 batch rows
-- and 7 tComm chunks), derive `∃ wTab, Satisfies` at the run's own sponge challenges — the
-- ft/quotient residue DISSOLVED (no `t`/`hteq`), the grid BYPASSED (fed the openings seam directly,
-- so no poseidon_fiat_shamir). Trust surface = kimchi_fiat_shamir (Poseidon-RO) + DL-binding (hbind)
-- + CompElliptic — and nothing else. The guarded conclusion (challenges avoid the negligible
-- bad sets) is the FS heuristic in the type; the honest-key hypotheses (hvk + omega/zkRows/shifts/endo
-- bridges) say the wire VK corresponds to the circuit. This is the endpoint (AGM, as in PLONK).
Kimchi.Verifier.kimchiVesta_run_sound_algebraic_ft
Kimchi.Verifier.kimchiPallas_run_sound_algebraic_ft
