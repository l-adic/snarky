-- Library API root set for the kimchi package.
--
-- The curated top-level surface the library exists to provide. Lean has no export-list
-- mechanism (everything non-`private` is visible once imported), so this manifest — not the
-- type system — defines the public API. It is the root set for dead-code analysis:
--
--   a declaration in `Kimchi.*` is LIVE iff it is in the transitive constant-dependency
--   closure of these roots; anything compiled but outside that closure is dead.
--
-- The list is a MINIMAL GENERATING SET: a result reachable from a listed root is deliberately
-- not listed (e.g. `Kimchi.Protocol.sound` and the openings seam
-- `kimchiProof_sound_of_openings` are live through every run-level root; `zH_dvd_iff` and
-- `dvd_separation` through the whole soundness line). `scripts/check_axioms.lean` separately
-- pins the existence and axiom closure of the headline theorems, including ones unlisted here.
--
-- Format: one fully-qualified declaration name per line; `--` lines and blanks are ignored.
--
-- Note: the `kimchi-demo` executable (Main.lean) is a SEPARATE build root, reaching the demo
-- defs `Kimchi.Gate.{egQ, egGood, egBad, satisfies}`. Those are demo-only and are not part
-- of this library API surface, so they are reachable through the executable rather than here.

-- Generic gate
Kimchi.Gate.Generic.sound
Kimchi.Gate.Generic.complete
Kimchi.Gate.satisfies_iff

-- Complete addition
Kimchi.Gate.AddComplete.complete
Kimchi.Gate.AddComplete.sound

-- Variable-base scalar multiplication: the gate completeness and the two deployed circuit
-- entry points (the gate soundness is live through `varBaseMul_scaleFast2`)
Kimchi.Gate.VarBaseMul.complete
Kimchi.Gate.VarBaseMul.varBaseMul_scaleFast1
Kimchi.Gate.VarBaseMul.varBaseMul_scaleFast2

-- Endo-scalar decomposition (circuit): over a multi-row run of sequential gates, the effective
-- scalar a·λ + b (sound), the honest prover fills the run (complete), and the decomposition is
-- unique under the bit-size/field-size bound (so challenge ↦ a·λ + b is well defined). The
-- per-gate `sound`/`complete` are live through `endoScalar_unique`/`chain_complete`.
Kimchi.Gate.EndoScalar.chain_toField
Kimchi.Gate.EndoScalar.chain_complete
Kimchi.Gate.EndoScalar.endoScalar_unique

-- GLV endomorphism scalar multiplication (gate)
Kimchi.Gate.EndoMul.sound
Kimchi.Gate.EndoMul.complete

-- EndoMul at the real Pasta curves: the deployed entry points. The prover supplies only the gate
-- constraint Holds per row + base + threading + initial conditions; the eigenvalue is discharged
-- via the CM axiom and the per-row hxne is *derived* from the GLV bound (not assumed). The
-- field-generic circuit capstone `endoMul` is live through both.
Kimchi.Gate.EndoMul.pallas_endoMul
Kimchi.Gate.EndoMul.vesta_endoMul

-- Poseidon gate: a satisfying row computes the 5-round permutation.
Kimchi.Gate.Poseidon.sound
Kimchi.Gate.Poseidon.complete

-- The arithmetization interface: satisfiability characterized by divisibility of the aggregate
-- family. (Its engine — `zH_dvd_iff`, `dvd_separation`, the `Argument` lifts — is live through
-- it and the soundness line.) Gated by `scripts/check_axioms.lean`.
Kimchi.Index.satisfies_iff_fullFamily_dvd

-- THE EXECUTABLE VERIFIER, over the checked records (production chunking; `nc = 1`
-- is just the one-chunk case), and the wire-boundary parser that feeds it: clients
-- parse the serde-typed wire records at the run's chunk count
-- (`Wire.{KimchiProof,KimchiVK}.check` — the verifier's own length checks factored
-- as a total parse; a checked record cannot hold a ragged proof) and call
-- `kimchiVerify` on the result. The composition is adjudicated against the
-- production fixtures — nc=2 on BOTH curves plus the one-chunk fixture — by
-- `scripts/check_kimchi_verifier.lean` (accept + corruption rejections).
Kimchi.Verifier.kimchiVerify
Kimchi.Verifier.Wire.KimchiProof.check
Kimchi.Verifier.Wire.KimchiVK.check
-- The reduction: the 44-row grid soundness at nc * 2^k = n (the public row is IN the
-- batch — its claims are proof-carried and believed only through binding), extraction
-- via chunked_batch_soundness directly.
Kimchi.Verifier.kimchiProof_sound
-- The standard-model run-level capstones (grid path): the grid capstones at the
-- deployed Pasta verifiers (per-node poseidon_fiat_shamir over the flat segment
-- stream) and the AGM corollary with the residue-free algebraic quotient (the ft
-- row's DOUBLE collapse at zeta^{2^k}).
Kimchi.Verifier.kimchiVesta_run_sound
Kimchi.Verifier.kimchiPallas_run_sound
-- The FS-reflection ft openings: from ONE genuine acceptance, the ft row of the run's
-- own flat stream opens to the computed runFtEval0 (axioms consumed: the
-- kimchi_fiat_shamir_* pair, once each).
Kimchi.Verifier.ft_opening_of_reflected_vesta
Kimchi.Verifier.ft_opening_of_reflected_pallas
Kimchi.Verifier.kimchiProof_sound_algebraic_ft
-- THE TERMINAL THEOREMS — AGM soundness of the deployed verifier at production
-- chunking. From a genuine `verify = true`, AGM representations of the 44*nc+1 flat
-- segment rows and the t chunks deliver the guarded `Satisfies idx (pubView idx pub)`:
-- openings seam fed directly, public row bound through the key's Lagrange chunk pin,
-- Maller identity from the ft opening at the DOUBLE zeta^{2^k} collapse. Trust
-- surface = kimchi_fiat_shamir (Poseidon-RO) + DL-binding (hbind) + CompElliptic.
Kimchi.Verifier.kimchiVesta_run_sound_algebraic_ft
Kimchi.Verifier.kimchiPallas_run_sound_algebraic_ft
