import CompElliptic.Fields.Pasta

/-!
# Pasta field names

The import point for upstream `CompElliptic/Fields/Pasta.lean`, whose
`CompElliptic.Fields.Pasta.Fp` and `CompElliptic.Fields.Pasta.Fq` (the Pallas base and scalar
fields) are the names this project uses. The point-group bridge to Mathlib's `Affine.Point` is
in `Pasta.Basic`.
-/
