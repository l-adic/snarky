-- API root set for the pasta package: the curve trust base (certified orders, the GLV
-- constants, the point-group instances, the wire scalar-shift algebra).
--
-- The package has no roots of its own: everything it proves is reached from the packages
-- above it — the kimchi gates and arithmetization, the snarky gadgets, the pickles
-- capstones — or from a CI check script through them.
