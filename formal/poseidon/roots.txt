-- API root set for the poseidon package: the permutation, the duplex sponge, the FqSponge
-- consumer layer and the SvdW map-to-curve, plus the shared fixture kit.

-- Invisible to the reachability walk, so rooted by hand: `rfl` simp lemmas (used from the
-- simp set, they leave no constant in a proof term), coercion instances the elaborator
-- applies, and the tactic implementations the completeness proofs call.
Poseidon.RandomOracle.hash_eq_squeeze

-- Consumed by the CI check scripts rather than by the library.
-- script-surface:begin
FixtureKit.Trace.check
Poseidon.squeezeN
Poseidon.FqSponge.challenge
Poseidon.FqSponge.squeezeChallenge
Poseidon.GroupMapPallas.toGroup
Poseidon.GroupMapVesta.toGroup
-- script-surface:end
