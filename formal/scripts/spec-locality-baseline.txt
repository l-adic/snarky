# By-design splits the spec-locality gate (scripts/check-spec-locality.sh) grandfathers:
# `file:gadget` pairs whose triple lives apart from the gadget's module on purpose. Each is
# the gadget's PRIMARY reading, in the module that owns the vocabulary it is read against —
# not a consumer's restatement. Never grow this for an assembly; state the shape the
# assembly needs beside the gadget instead (`builder_spec_forall`, `builder_spec_and`).

# The token interpreter is monad-generic (Linearization/Interpreter.lean); its circuit
# instance's reading is the linearization circuit's own result.
pickles/Pickles/Linearization/Circuit.lean:evalLoop
pickles/Pickles/Linearization/Circuit.lean:evaluate
# The reflection theorems read the generic interpreter at the certified deployed streams.
pickles/Pickles/Reflect/Soundness.lean:evaluate
# The x_hat wire crossing (`xHat_reads_publicCommitment`) is stated beside the wire
# vocabulary it crosses to, not beside the gadget.
pickles/Pickles/XhatGlue.lean:publicInputCommitFull
