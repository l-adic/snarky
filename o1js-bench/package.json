{
  "name": "o1js-bench",
  "private": true,
  "version": "0.1.0",
  "description": "Apples-to-apples benchmark of o1js against the snarky-PS pickles-bench workload (same logical programs, same measurement pipeline).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "bench": "node --trace-gc dist/bench.js",
    "rows": "node dist/rows.js"
  },
  "dependencies": {
    "o1js": "2.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
