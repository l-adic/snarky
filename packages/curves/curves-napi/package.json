{
  "name": "curves-napi",
  "version": "0.1.0",
  "main": "curves-napi.node",
  "napi": {
    "binaryName": "curves-napi",
    "triples": {}
  },
  "scripts": {
    "build": "napi build --release",
    "build:arkworks": "napi build --release --features arkworks",
    "build:mina-curves": "napi build --release --no-default-features --features mina-curves-backend",
    "prepublish": "npm run build"
  },
  "files": [
    "curves-napi.node"
  ]
}
