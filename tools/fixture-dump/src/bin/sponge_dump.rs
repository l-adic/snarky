//! Kimchi Poseidon sponge data for the Lean formalization (`formal/Kimchi/Sponge`).
//!
//! Emits two artifacts from `mina_poseidon`:
//!
//! * `PoseidonConstantsFq.lean` — the `fq_kimchi` parameters (55 × 3 round constants and the
//!   3 × 3 MDS matrix over the Vesta base field), as canonical `Nat` literals. Committed to
//!   `formal/Kimchi/Sponge/`; regenerate here on a proof-systems bump.
//! * `poseidon_fq_vectors.json` — absorb/squeeze traces of `ArithmeticSponge` over the same
//!   parameters (deterministic seeded inputs), the test vectors for the Lean sponge
//!   automaton.
//! * `fq_sponge_vectors.json` — op traces of `DefaultFqSponge<VestaParameters>` (scalar and
//!   point absorption, raw squeezes, 128-bit and endo-expanded challenges).
//! * `group_map_vectors.json` — `t ↦ to_group(t)` vectors of the SvdW map.
//!
//! Usage: `sponge_dump <lean-out-dir> <fixtures-out-dir>`.

use ark_ff::UniformRand;
use mina_curves::pasta::Fq;
use mina_poseidon::{
    constants::PlonkSpongeConstantsKimchi as SC,
    pasta::{fq_kimchi, FULL_ROUNDS},
    poseidon::{ArithmeticSponge, Sponge},
};
use rand::SeedableRng;
use rand_chacha::ChaCha20Rng;
use serde_json::json;
use std::fmt::Write as _;

fn fe<F: std::fmt::Display>(x: &F) -> String {
    format!("{}", x)
}

fn lean_matrix(name: &str, doc: &[&str], rows: &[[Fq; 3]]) -> String {
    let mut s = String::new();
    writeln!(s, "/-- {}", doc[0]).unwrap();
    for line in &doc[1..] {
        writeln!(s, "{line}").unwrap();
    }
    writeln!(s, "-/").unwrap();
    writeln!(s, "def {name} : Array (Array Nat) := #[").unwrap();
    for (i, row) in rows.iter().enumerate() {
        let sep = if i + 1 == rows.len() { "]" } else { "," };
        writeln!(s, "  #[{},", fe(&row[0])).unwrap();
        writeln!(s, "    {},", fe(&row[1])).unwrap();
        writeln!(s, "    {}]{}", fe(&row[2]), sep).unwrap();
    }
    s
}

fn main() {
    let mut args = std::env::args().skip(1);
    let lean_dir = args
        .next()
        .expect("usage: poseidon_dump <lean-out-dir> <fixtures-out-dir>");
    let fix_dir = args
        .next()
        .expect("usage: poseidon_dump <lean-out-dir> <fixtures-out-dir>");

    let params = fq_kimchi::static_params();

    // --- PoseidonConstantsFq.lean ---
    let mut lean = String::new();
    writeln!(
        lean,
        "/-!\n\
         # Kimchi Poseidon parameters over the Vesta base field\n\
         \n\
         The `fq_kimchi` sponge parameters of proof-systems `mina_poseidon` (width 3, rate 2,\n\
         55 full rounds, `x^7` S-box, full MDS, no initial ARK), as canonical `Nat` values.\n\
         \n\
         Generated by `tools/fixture-dump` (`sponge_dump`); do not edit by hand.\n\
         -/\n\
         \n\
         namespace Kimchi.Sponge.FqKimchi\n"
    )
    .unwrap();
    lean.push_str(&lean_matrix(
        "roundConstants",
        &[
            "The 55 × 3 round constants: `roundConstants[r][i]` is added to state element",
            "`i` at the end of round `r`.",
        ],
        &params.round_constants,
    ));
    lean.push('\n');
    lean.push_str(&lean_matrix(
        "mds",
        &["The 3 × 3 MDS matrix, row-major."],
        &[params.mds[0], params.mds[1], params.mds[2]],
    ));
    lean.push_str("\nend Kimchi.Sponge.FqKimchi\n");
    let lean_path = format!("{lean_dir}/PoseidonConstantsFq.lean");
    std::fs::write(&lean_path, lean).unwrap();
    println!("wrote {lean_path}");

    // --- absorb/squeeze test vectors ---
    // Each case is a list of operations; `absorb` carries inputs, `squeeze` carries the
    // expected outputs. Cases cover: squeeze-from-fresh, partial and full rate absorption,
    // multi-block absorption, multi-squeeze, and absorb-after-squeeze.
    let rng = &mut ChaCha20Rng::from_seed([44u8; 32]);
    let shapes: Vec<Vec<(&str, usize)>> = vec![
        vec![("squeeze", 1)],
        vec![("absorb", 1), ("squeeze", 1)],
        vec![("absorb", 2), ("squeeze", 1)],
        vec![("absorb", 3), ("squeeze", 2)],
        vec![("absorb", 5), ("squeeze", 4)],
        vec![("absorb", 1), ("squeeze", 1), ("absorb", 1), ("squeeze", 3)],
        vec![("squeeze", 3), ("absorb", 2), ("squeeze", 1)],
    ];
    let cases: Vec<_> = shapes
        .iter()
        .map(|shape| {
            let mut sponge =
                ArithmeticSponge::<Fq, SC, FULL_ROUNDS>::new(fq_kimchi::static_params());
            let ops: Vec<_> = shape
                .iter()
                .map(|(op, count)| match *op {
                    "absorb" => {
                        let xs: Vec<Fq> = (0..*count).map(|_| Fq::rand(rng)).collect();
                        sponge.absorb(&xs);
                        json!({ "op": "absorb", "values": xs.iter().map(fe).collect::<Vec<_>>() })
                    }
                    _ => {
                        let outs: Vec<Fq> = (0..*count).map(|_| sponge.squeeze()).collect();
                        json!({ "op": "squeeze", "expect": outs.iter().map(fe).collect::<Vec<_>>() })
                    }
                })
                .collect();
            json!({ "ops": ops })
        })
        .collect();

    let vectors = json!({ "field": "fq_kimchi", "cases": cases });
    let vec_path = format!("{fix_dir}/poseidon_fq_vectors.json");
    std::fs::write(&vec_path, serde_json::to_string_pretty(&vectors).unwrap()).unwrap();
    println!("wrote {vec_path} ({} cases)", shapes.len());

    // --- DefaultFqSponge op-trace vectors ---
    // Traces over the consumer-facing sponge: scalar and point absorption, raw field
    // squeezes, 128-bit prechallenges, and endo-expanded effective challenges. Covers the
    // limb buffer across consecutive challenges and each absorb kind resetting it.
    {
        use ark_ec::CurveGroup;
        use mina_curves::pasta::{Fp, Vesta};
        use mina_poseidon::{sponge::ScalarChallenge, FqSponge};
        use poly_commitment::ipa::endos;

        type FqS = mina_poseidon::sponge::DefaultFqSponge<
            mina_curves::pasta::VestaParameters,
            SC,
            FULL_ROUNDS,
        >;
        let rng = &mut ChaCha20Rng::from_seed([45u8; 32]);
        let (_, endo_r) = endos::<Vesta>();
        let rand_pt =
            |rng: &mut ChaCha20Rng| (<Vesta as ark_ec::AffineRepr>::Group::rand(rng)).into_affine();
        let shapes: Vec<Vec<&str>> = vec![
            vec!["challenge"],
            vec!["absorb_fr", "challenge"],
            vec!["absorb_fr", "challenge_fq"],
            vec!["absorb_g", "squeeze_challenge"],
            vec!["absorb_g", "challenge", "challenge"],
            vec![
                "absorb_fr",
                "challenge",
                "absorb_g",
                "squeeze_challenge",
                "challenge_fq",
            ],
            vec!["challenge_fq", "absorb_fr", "squeeze_challenge"],
        ];
        let cases: Vec<_> = shapes
            .iter()
            .map(|shape| {
                let mut sp = FqS::new(fq_kimchi::static_params());
                let ops: Vec<_> = shape
                    .iter()
                    .map(|op| match *op {
                        "absorb_fr" => {
                            let x = Fp::rand(rng);
                            sp.absorb_fr(&[x]);
                            json!({ "op": "absorb_fr", "value": fe(&x) })
                        }
                        "absorb_g" => {
                            let p = rand_pt(rng);
                            sp.absorb_g(&[p]);
                            json!({ "op": "absorb_g", "point": pt(&p) })
                        }
                        "challenge_fq" => {
                            json!({ "op": "challenge_fq", "expect": fe(&sp.challenge_fq()) })
                        }
                        "challenge" => {
                            json!({ "op": "challenge", "expect": fe(&sp.challenge()) })
                        }
                        _ => {
                            let c = ScalarChallenge::new(sp.challenge()).to_field(&endo_r);
                            json!({ "op": "squeeze_challenge", "expect": fe(&c) })
                        }
                    })
                    .collect();
                json!({ "ops": ops })
            })
            .collect();
        let vectors = json!({ "curve": "vesta", "cases": cases });
        let p = format!("{fix_dir}/fq_sponge_vectors.json");
        std::fs::write(&p, serde_json::to_string_pretty(&vectors).unwrap()).unwrap();
        println!("wrote {p} ({} cases)", shapes.len());
    }

    // --- group_map vectors ---
    // `t ↦ to_group(t)` at seeded field elements, from the production map.
    {
        use groupmap::GroupMap;
        let rng = &mut ChaCha20Rng::from_seed([46u8; 32]);
        let gm = groupmap::BWParameters::<mina_curves::pasta::VestaParameters>::setup();
        let vectors: Vec<_> = (0..8)
            .map(|_| {
                let t = Fq::rand(rng);
                let (x, y) = gm.to_group(t);
                json!({ "t": fe(&t), "x": fe(&x), "y": fe(&y) })
            })
            .collect();
        let out = json!({ "curve": "vesta", "vectors": vectors });
        let p = format!("{fix_dir}/group_map_vectors.json");
        std::fs::write(&p, serde_json::to_string_pretty(&out).unwrap()).unwrap();
        println!("wrote {p} (8 vectors)");
    }
}

fn pt(g: &mina_curves::pasta::Vesta) -> serde_json::Value {
    use ark_ec::AffineRepr;
    assert!(!g.is_zero(), "vector point unexpectedly at infinity");
    json!([fe(&g.x), fe(&g.y)])
}
